//
// CozyDreams Theme Exports
//

import pastelPalette from "./colors";

// Placeholder for future: darkTheme, holidayTheme, etc.
export const theme = {
  palette: pastelPalette,
  // Add shadows, radii, spacing scales here as needed
  borderRadius: "22px",
  cardShadow: "0 8px 32px rgba(187, 153, 255, 0.16)",
  fontFamily: `'Poppins', 'Segoe UI', 'Comic Sans MS', cursive, sans-serif`,
};

export default theme;
